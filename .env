# WebSocket configuration (WS / WSS)
WS_PROTOCOL=ws
WS_HOST=10.55.55.100
WS_PORT=25001
WS_PATH=/graphql
WS_URL=${WS_PROTOCOL}://${WS_HOST}:${WS_PORT}${WS_PATH}

# HTTP configuration (HTTP / HTTPS)
HTTP_PROTOCOL=http
HTTP_HOST=10.55.55.100
HTTP_PORT=25001
HTTP_BASE_PATH=/graphql
HTTP_URL=${HTTP_PROTOCOL}://${HTTP_HOST}:${HTTP_PORT}${HTTP_BASE_PATH}

# Real time video / audio call
RTC_PROTOCOL=ws
RTC_HOST=10.55.55.100
RTC_PORT=25101
RTC_PATH=
RTC_URL=${RTC_PROTOCOL}://${RTC_HOST}:${RTC_PORT}${RTC_PATH}