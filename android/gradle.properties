org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true

# AndroidX 必备
android.useAndroidX=true
android.enableJetifier=true

# 关键：这三个才是真正加速构建的
org.gradle.daemon=true          # 开 Gradle 守护进程，下次 build 会快很多
org.gradle.parallel=true        # 并行编译 module
org.gradle.configureondemand=true  # 按需配置，减少无关 module 的开销